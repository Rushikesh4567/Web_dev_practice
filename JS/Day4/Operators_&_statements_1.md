🟢 EASY LEVEL
1.Print the result of:
    -let a = 10, b = 3;
    -a + b
    -a % b
    -a ** b
2.Use increment operator:
    let x = 5;

    Print value after:
        x++
        ++x
3.Check if a number is even or odd.
4.Compare two numbers:
    Print which is greater

5.Use == and === and observe difference.