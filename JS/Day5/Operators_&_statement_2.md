🟡 MEDIUM LEVEL
1.Take a number and check:
    👉 Positive, Negative, or Zero
2.Check if a number is divisible by:
    3 and 5
3.Write a program:
    👉 Find the largest among 3 numbers
4.Convert if-else into ternary:
    if (marks >= 40) {
    console.log("Pass");
    } else {
    console.log("Fail");
    }
5.Take user input:
    👉 Check if number is multiple of 5
(Hint: prompt())